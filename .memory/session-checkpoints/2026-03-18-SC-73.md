# Session Checkpoint — SC-73

**Date:** 2026-03-18T10:40:00Z
**Task:** SC-73 — Add merchant trust promotion flow to Watch page
**Result:** Done ✅
**Commit:** f2c16ba → main

## What Changed

### Backend (`backend/src/szimplacoffee/api/merchants.py`)
- Added `VALID_TRUST_TIERS = {"trusted", "verified", "candidate", "rejected"}` constant
- Added `MerchantUpdateRequest` Pydantic model with optional `trust_tier` field
- Added `PATCH /api/v1/merchants/{merchant_id}` endpoint that validates the new tier and updates the DB row

### Frontend (`frontend/src/hooks/use-watchlist.ts`)
- Added `useUpdateTrustTier()` TanStack Query mutation hook calling `PATCH /api/v1/merchants/{id}`
- Invalidates all `["merchants"]` queries on success

### Frontend (`frontend/src/routes/watch.lazy.tsx`)
- Defined `TRUST_TIER_ORDER = ["rejected", "candidate", "verified", "trusted"]` progression
- Added `TrustControls` component rendering ↑ (promote) and ↓ (demote) buttons based on current tier position
- Promote button shown when not already at "trusted"; demote when not at "rejected"
- Buttons integrated into both Watchlist and Review Queue tabs alongside existing actions
- Toast feedback on success

## Verification
- `ruff check`: All checks passed
- `pytest tests/ -q`: 129 passed (up from 84 — prior sprint added more tests)
- `npm run build`: ✓ built in 4.05s, no errors

## Notes
- Watch page trust_tier display uses "trusted/verified/candidate/rejected" labels matching DB model
- TrustBadge already had color classes for all four tiers — no update needed there
- The PATCH endpoint is minimal: only trust_tier for now, easy to extend for other fields
- Next: SC-72 (wire brew feedback scores into recommendation ranking)
