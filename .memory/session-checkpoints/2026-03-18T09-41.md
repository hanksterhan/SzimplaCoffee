# Autopilot Checkpoint — 2026-03-18T09:41:00Z

## Run Result
success

## Action Taken
deliver SC-74

## Task
SC-74 — Add crawl health fields to merchant API (last_crawl_at, product_count, metadata_pct)

## Notes
- No pending approvals. Brainstorm packet fresh (18h old). 12 ready tasks available.
- Selected SC-74 over SC-59/SC-67/SC-73 because it unblocks SC-79 (Watch page UI) and is the smallest safe backend-only slice.
- Added `last_crawl_at`, `crawl_success`, `product_count`, `metadata_pct` to `MerchantSummary` schema (pydantic).
- Implemented `_enrich_merchant_summaries()` helper using two SQLAlchemy subqueries: latest CrawlRun per merchant, product count + metadata fill rate via CASE expression.
- Applied enrichment to `list_merchants`, `list_watchlist`, and `list_low_confidence_merchants` endpoints.
- 6 new pytest tests: covers null crawl run, failed crawl, metadata counting (process/roast/origin), latest-run selection, and watchlist enrichment.
- Regenerated `frontend/src/api/schema.d.ts` via live openapi endpoint on port 8001.
- Frontend build clean (Vite 4.39s). 129 tests pass, ruff clean.
- Committed and pushed to main (e72de52).
- SC-74 moved to .tickets/closed/.

## Next Run Hint
SC-79 (Watch page crawl health UI rendering) is now unblocked. Also consider SC-67 (explain mode) or SC-59 (bulk import CLI).
