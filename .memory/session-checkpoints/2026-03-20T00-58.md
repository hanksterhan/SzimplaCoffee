# Autopilot Checkpoint — 2026-03-20T00:58:00Z

## Run Result
success

## Action Taken
deliver SC-98

## Task
SC-98 — Expose deal baselines in catalog API and Today view

## Notes
Selected SC-98 (p1) over SC-99 (p2). Both were ready with no blockers.

Delivered in two slices:

**S1 — Backend:**
- Added `DealFactSchema` (baseline_30d_cents, price_drop_30d_percent,
  compare_at_discount_percent, historical_low_cents) to schemas/products.py
- Added `deal_fact: Optional[DealFactSchema] = None` to `ProductSummary`
- Added `_deal_fact_schema_from_variant` helper in api/products.py
- Updated selectinload chains in merchant-products, global search, and detail endpoints
  to eagerly load `ProductVariant.deal_fact`
- Added 4 new tests (deal_fact_client fixture + 3 test functions); 274/274 pass

**S2 — Frontend:**
- Started backend, ran `npm run gen:api` to regenerate schema.d.ts with deal_fact fields
- Added `DealBadge` component in products.lazy.tsx
- Badge shows `↓N% vs 30d avg` when price_drop_30d_percent > 5
- Fallback badge `Save N%` when compare_at_discount_percent > 0
- Badge added to ProductCard image overlay and ProductQuickView price row
- Null-safe: when deal_fact is null/absent, nothing renders
- Frontend build and tsc -b clean

Committed to stage, pushed to origin.

## Next Run Hint
SC-99 (quality sort, p2) is the only remaining ready task — backend-only change,
no API gen needed. Safe for next run.
