# Autopilot Checkpoint — 2026-03-19T03:00:00Z

## Run Result
stopped

## Action Taken
none — blocked on pending approval

## Task
SC-85 (blocked pending human decision)

## Notes
Approval gate (Step 2) fired immediately. Pending entry `appr_1773867960000` exists in
`autopilot/approvals.jsonl`. This is the 7th consecutive blocked run on the same approval.

SC-85 cannot satisfy AC-1 (≥70% origin_country fill rate across ALL products) because
~130 rows are non-coffee SKUs (subscriptions, merchandise) that will never have origin data.
Current fill rate is ~63% even after demonym mapping improvements from prior runs.

**Human decision needed — choose one:**

**Option A (Scope expansion — requires approval):** Store description_html in crawl, re-crawl
all merchants, run backfill. This would surface more origin text but is a larger change.

**Option B (AC denominator fix — recommended):** Update SC-85 AC-1 to measure fill rate on
coffee products only (products where product_type = 'coffee' or name matches coffee keywords).
This is correct — non-coffee SKUs should not count against the origin fill rate goal.
To implement: edit AC-1 in SC-85.yaml, then set `appr_1773867960000` status to `resolved`
in `autopilot/approvals.jsonl`.

**Option C (Pivot):** Set SC-85 status to `blocked` in its YAML, set approval
`appr_1773867960000` to `resolved`. Next run will pick SC-86 (seed 5 purchases to reach
≥10 total logged).

## Next Run Hint
Human must resolve `appr_1773867960000` before any progress. Recommended: Option B — update
AC-1 denominator to coffee-only, resolve the approval, next run delivers SC-85 properly.
