# Autopilot Checkpoint — 2026-03-20T01:52:00Z

## Run Result
success

## Action Taken
deliver SC-99 (after backlog refill: SC-100 + SC-101 created)

## Task
SC-99 — Add quality-first server-side product sort mode

## Notes
Backlog had only 1 ready ticket (SC-99) against min_ready_tasks=2. Created SC-100 (Today view deal badges) and SC-101 (merchant expansion) from brainstorm candidates to refill before delivering.

SC-99 was partially implemented from a prior run (backend changes already on feat/SC-99-quality-sort branch). Completed the remaining work:
- Discovered `overall_quality_score` is on `MerchantQualityProfile`, not `Merchant` directly — plan was inaccurate on this point. Fixed by adding `selectinload(Merchant.quality_profile)` to both catalog endpoints.
- Added new test fixture `quality_sort_client` with 2 merchants at different quality scores (0.9 vs 0.3) and verified ordering: in-stock HQ first, in-stock LQ second, sold-out last.
- Added "Quality: best first" to frontend sort selector.
- Updated schema.d.ts manually (backend not running).

Verification: 275 backend tests pass (+1 new), frontend build and tsc -b clean.
Branch merged to stage and pushed. Feature branch deleted.

## Next Run Hint
SC-100 (Today view deal badges) is ready — frontend-only, depends on inspecting use-today hook for deal_fact availability. SC-101 (merchant expansion via CLI) is also ready but requires serialized `szimpla add-merchant` runs (data operation only). SC-100 is lower-risk code change; deliver that next.
