# Session Checkpoint: 2026-03-18T15:30Z

## Delivered
**SC-71** — Global brew-feedback list endpoint + 3 tests

## What Changed
- `backend/src/szimplacoffee/api/history.py`: Added `GET /api/v1/history/brew-feedback` endpoint that returns all BrewFeedback rows across all purchases (newest-first), with `limit` query param (max 500).
- `backend/tests/test_history.py`: Added 3 new tests: seeded rows returned, new row included, all rows have rating+notes populated.

## Verification
- AC-1: brew_feedback count = 3 ✅
- AC-2: GET /api/v1/history/brew-feedback returns 3 items ✅  
- AC-3: All rows have rating (10.0, 7.0, 8.5) and notes populated ✅
- 156 backend tests green (was 153)
- ruff clean

## Key Observation
The brew_feedback data was already seeded from bootstrap (3 real sessions via bootstrap.py).
The missing piece was a global list endpoint — nested per-purchase endpoints existed but SC-71 required a global view.

## Next
SC-72 (wire brew feedback into recommendation scoring) is now unblocked.
SC-78 (PurchaseForm recommendationRunId prop) is also ready and unblocked.
