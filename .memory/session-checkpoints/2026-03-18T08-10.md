# Session Checkpoint — 2026-03-18T08:10 UTC

## Task Delivered
**SC-66**: Add recommendation engine regression test with seeded offer_snapshot data

## What Changed
- `backend/tests/test_recommendations.py`: added 3 new tests + 2 new imports (`build_recommendations`, `build_wait_assessment`)

## Tests Added
1. `test_build_recommendations_returns_candidate_with_seeded_offer_snapshot` — calls `build_recommendations()` directly with the `deal_test_client` fixture; asserts ≥1 candidate, all scores > 0
2. `test_recommendations_endpoint_returns_ranked_results_with_seeded_data` — POST `/api/v1/recommendations`; handles actual `{top_result, alternatives}` response shape (not `results`/`recommendations`)
3. `test_wait_is_false_when_inventory_is_zero` — asserts `build_wait_assessment` returns `wait=False` when `current_inventory_grams=0` and candidates exist

## Surprises / Sharp Edges
- `RecommendationRequest` requires `shot_style`, `quantity_mode`, `bulk_allowed` as positional — no defaults. Tests must provide all three.
- `POST /api/v1/recommendations` returns HTTP 201, not 200.
- Response shape is `{top_result, alternatives, wait_recommendation, wait_rationale, run_id}` — not a flat `results` list.

## Verification
- 123 pytest tests pass (was 120 before SC-66)
- ruff check clean on modified files
- Committed and pushed to main: 6d69b8e

## Next Recommended Task
SC-75 — Crawl batch 2 (Blue Bottle, Stumptown, Heart, George Howell, Passenger)
