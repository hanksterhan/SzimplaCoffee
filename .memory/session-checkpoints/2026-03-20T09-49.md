# Autopilot Checkpoint — 2026-03-20T09:49:00Z

## Run Result
success

## Action Taken
deliver SC-109

## Task
SC-109 — Today view deal-score: rank quality-clearing deals using historical price baseline

## Notes
Selected SC-109 (p1) over SC-108 (p2). SC-107 blocker was already resolved.

Delivered all three slices:

**S1 (Backend):** Added `_baseline_deal_score()` to recommendations.py — queries
VariantPriceBaseline and computes deal_score clamped [-1,1], assigns deal_badge.
Blended ranking formula: `total = 0.7 * base_composite + 0.3 * max(0, deal_score)`.
Quality-dominant; deal is tiebreaker. Added deal-aware wait signal for borderline quality
candidates priced above baseline. Added deal_score/deal_badge fields to RecommendationCandidate
and RecommendationCandidateOut.

**S2 (Tests):** New test_deal_score.py — 19 tests covering deal_score computation, badge
boundaries (at exact 15%, 5% thresholds), clamping, no_baseline, blend constants, and
blended ranking ordering. Fixed 3 existing brew_feedback tests whose score assertions
assumed direct arithmetic; now correctly account for QUALITY_BLEND_WEIGHT=0.7 multiplier.

**S3 (Frontend):** Added BaselineDealBadge component to today.lazy.tsx; renders 🔥/✓/↑
badges on TopPickCard and RunnerUpCard. Updated TodayRecommendationCandidate interface
with deal_score/deal_badge fields.

**Verification:**
- `pytest tests/ -q`: 327 passed (0 failed)
- `npm run build`: clean, today.lazy 10.00 kB
- `npx tsc -b`: no errors
- `ruff check src/ tests/`: all checks passed
- Merged SC-109-today-deal-score → stage, pushed to remote.

## Next Run Hint
completed_since_reflect=6 ≥ reflection threshold of 5 — next run should trigger reflection first.
After reflection: SC-108 (catalog quality sort, p2) is the only remaining open ticket.
