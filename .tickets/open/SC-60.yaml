id: SC-60
title: "Review and promote crawled merchants based on crawl quality"
type: task
priority: p1
status: ready
owner: h6nk-bot
created_at: "2026-03-18T00:00:00Z"
updated_at: "2026-03-18T09:11:00Z"
labels: ["merchant-coverage", "epic-1", "quality"]

problem: "After initial crawls, all new merchants sit at default trust_tier=C. High-quality merchants with successful crawls and rich product data should be promoted to Tier A or B so they appear in recommendations."
desired_outcome: "Crawled merchants are reviewed against quality profiles. Tier A candidates are promoted. Trust and crawl tier assignments are documented."

scope_in:
  - "Run szimpla score-merchants to regenerate quality profiles"
  - "Review MerchantQualityProfile scores for new merchants"
  - "Promote merchants with score >= 0.7 to crawl_tier=A"
  - "Set trust_tier=B for merchants with consistent product data"
  - "Update crawl_tier=D for merchants with zero products after 2 crawl attempts"
scope_out:
  - "Fixing broken crawl adapters (separate ticket)"
  - "Changes to quality scoring algorithm (requires its own ticket)"

dependencies:
  blockers: ["SC-58"]
  unblocks: []

acceptance_criteria:
  - id: AC-1
    text: "At least 3 merchants have been promoted to crawl_tier A or B"
    verify: "cd backend && python -c \"from szimplacoffee.db import engine; from sqlalchemy import text; print(engine.connect().execute(text(\\\"SELECT COUNT(*) FROM merchants WHERE crawl_tier IN ('A','B')\\\")).scalar())\""
    status: pending
  - id: AC-2
    text: "Merchants with zero products after crawl are set to crawl_tier=D"
    verify: "cd backend && python -c \"from szimplacoffee.db import engine; from sqlalchemy import text; print(engine.connect().execute(text('SELECT m.domain, m.crawl_tier FROM merchants m LEFT JOIN products p ON p.merchant_id=m.id GROUP BY m.id HAVING COUNT(p.id)=0')).fetchall())\""
    status: pending
  - id: AC-3
    text: "quality_scorer runs without exceptions via CLI"
    verify: "cd backend && . .venv/bin/activate && szimpla score-merchants 2>&1 | tail -5"
    status: pending

verification_required:
  - "ruff check src/ tests/"
  - "pytest tests/ -v"

context_refs:
  - "/home/h6nk/SzimplaCoffee/.plans/SC-60-execution-plan.md"

slices:
  - id: S1
    title: "Run score-merchants and inspect quality profiles"
    status: pending
    depends_on: []
    files_create: []
    files_modify: []
    files_read_only:
      - "backend/src/szimplacoffee/services/quality_scorer.py"
    checks:
      - "merchant_quality_profiles table populated"
    evidence: []
    notes: ""
  - id: S2
    title: "Apply tier promotions and demotions"
    status: pending
    depends_on: ["S1"]
    files_create: []
    files_modify:
      - "backend/src/szimplacoffee/services/quality_scorer.py"
    files_read_only: []
    checks:
      - "tier assignments match quality scores"
    evidence: []
    notes: "May require adding auto-promote logic to quality_scorer.py"

delivery:
  branch: ""
  commits: []
  pr_url: ""
  started_at: ""
  completed_at: ""
  verification:
    required_passed: false
    commands_run: []
    evidence_refs: []

notes:
  - "Tier A = crawl every 6h; B = 24h; C = 7d; D = excluded from recommendations"
