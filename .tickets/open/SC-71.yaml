id: SC-71
title: "Log 3 real brew sessions with rating and notes via BrewFeedbackForm"
type: task
priority: p2
status: ready
owner: h6nk-bot
created_at: "2026-03-18T00:00:00Z"
updated_at: "2026-03-18T00:00:00Z"
labels: ["purchase-loop", "epic-4", "brew-feedback"]

problem: "The BrewFeedbackForm exists but has never been exercised with real data. Without brew feedback records the recommendation engine cannot penalize poorly-rated products."
desired_outcome: "At least 3 brew sessions are logged through the BrewFeedbackForm with rating and notes. Each row appears in brew_feedback table and is retrievable via the history API."

scope_in:
  - "Exercise BrewFeedbackForm with 3 real brew sessions"
  - "Include rating (1-5) and notes for each"
  - "Verify rows appear in brew_feedback table"
  - "Verify rows returned by GET /api/v1/history/brew-feedback"
  - "Fix any form submission errors discovered"
scope_out:
  - "Wiring feedback into recommendation ranking (SC-72)"
  - "UI redesign of the form"
  - "Purchase linkage"

dependencies:
  blockers: ["SC-69"]
  unblocks: ["SC-72"]

acceptance_criteria:
  - id: AC-1
    text: "brew_feedback table has at least 3 rows"
    verify: "cd backend && python -c \"from szimplacoffee.db import engine; from sqlalchemy import text; print(engine.connect().execute(text('SELECT COUNT(*) FROM brew_feedback')).scalar())\""
    status: pending
  - id: AC-2
    text: "GET /api/v1/history/brew-feedback returns at least 3 items"
    verify: "curl -s http://localhost:8000/api/v1/history/brew-feedback | python -c \"import json,sys; d=json.load(sys.stdin); items=d if isinstance(d,list) else d.get('items',d.get('feedback',[])); print(len(items))\""
    status: pending
  - id: AC-3
    text: "All rows have rating and notes fields populated"
    verify: "cd backend && python -c \"from szimplacoffee.db import engine; from sqlalchemy import text; print(engine.connect().execute(text('SELECT rating, notes FROM brew_feedback LIMIT 3')).fetchall())\""
    status: pending

verification_required:
  - "ruff check src/ tests/"
  - "pytest tests/ -v"

context_refs:
  - "/home/h6nk/SzimplaCoffee/.plans/SC-71-execution-plan.md"

slices:
  - id: S1
    title: "Exercise BrewFeedbackForm and fix submission errors"
    status: pending
    depends_on: []
    files_create: []
    files_modify:
      - "frontend/src/components/purchases/BrewFeedbackForm.tsx"
      - "backend/src/szimplacoffee/api/history.py"
    files_read_only: []
    checks:
      - "3 rows in brew_feedback"
      - "API returns those rows"
    evidence: []
    notes: "Use purchases from SC-69 as the linked context for brew sessions"

delivery:
  branch: ""
  commits: []
  pr_url: ""
  started_at: ""
  completed_at: ""
  verification:
    required_passed: false
    commands_run: []
    evidence_refs: []

notes:
  - "Document any form UX issues discovered as follow-up tickets"
