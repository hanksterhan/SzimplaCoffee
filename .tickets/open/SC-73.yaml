id: SC-73
title: "Add merchant trust promotion flow to Watch page"
type: feature
priority: p2
status: ready
owner: h6nk-bot
created_at: "2026-03-18T00:00:00Z"
updated_at: "2026-03-18T00:00:00Z"
labels: ["watch-queue", "epic-5", "ux"]

problem: "The Watch page shows merchants in the review queue but there is no way to promote or demote a merchant's trust tier from the UI. This forces manual DB edits to adjust trust tier."
desired_outcome: "Each merchant card on the Watch page has promote and demote buttons. Clicking promote/demote calls PATCH /api/v1/merchants/{id} and updates the trust_tier. The card reflects the new tier immediately."

scope_in:
  - "Add promote/demote buttons to merchant cards in watch.lazy.tsx"
  - "Wire buttons to PATCH /api/v1/merchants/{id} endpoint"
  - "Ensure PATCH endpoint accepts trust_tier updates"
  - "Refresh card state after successful PATCH"
scope_out:
  - "Crawl tier changes from UI (crawl health is SC-74)"
  - "Bulk promotion workflow"
  - "Audit trail for trust tier changes"

dependencies:
  blockers: []
  unblocks: []

acceptance_criteria:
  - id: AC-1
    text: "Watch page renders promote and demote buttons on each merchant card"
    verify: "cd frontend && npm run build 2>&1 | tail -3"
    status: pending
  - id: AC-2
    text: "PATCH /api/v1/merchants/{id} accepts trust_tier field and updates it"
    verify: "curl -s -X PATCH http://localhost:8000/api/v1/merchants/1 -H 'Content-Type: application/json' -d '{\"trust_tier\": \"B\"}' | python -c \"import json,sys; d=json.load(sys.stdin); print(d.get('trust_tier'))\""
    status: pending
  - id: AC-3
    text: "Card trust_tier label updates after promote/demote without page reload"
    verify: "cd frontend && npm run build 2>&1 | grep -i error | head -5 || echo 'build clean'"
    status: pending

verification_required:
  - "ruff check src/ tests/"
  - "pytest tests/ -v"
  - "cd frontend && npm run build"

context_refs:
  - "/home/h6nk/SzimplaCoffee/.plans/SC-73-execution-plan.md"

slices:
  - id: S1
    title: "Ensure PATCH /api/v1/merchants/{id} supports trust_tier"
    status: pending
    depends_on: []
    files_create: []
    files_modify:
      - "backend/src/szimplacoffee/api/merchants.py"
    files_read_only: []
    checks:
      - "PATCH accepts trust_tier"
      - "DB row updated"
    evidence: []
    notes: "If endpoint does not exist, create it following existing pattern"
  - id: S2
    title: "Add promote/demote buttons to Watch page merchant cards"
    status: pending
    depends_on: ["S1"]
    files_create: []
    files_modify:
      - "frontend/src/routes/watch.lazy.tsx"
    files_read_only: []
    checks:
      - "npm run build passes"
      - "buttons visible on cards"
      - "PATCH called on click"
    evidence: []
    notes: "Use TanStack Query mutation; invalidate merchant query on success"

delivery:
  branch: ""
  commits: []
  pr_url: ""
  started_at: ""
  completed_at: ""
  verification:
    required_passed: false
    commands_run: []
    evidence_refs: []

notes:
  - "Trust tiers: A (highest), B, C, D (excluded from recommendations)"
