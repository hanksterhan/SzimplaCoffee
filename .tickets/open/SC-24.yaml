id: SC-24
title: "Add merchant page — URL input, platform auto-detection, crawl trigger"
type: feature
priority: p2
status: open
owner: h6nk-bot
created_at: "2026-03-14T00:00:00Z"
updated_at: "2026-03-14T00:00:00Z"
labels: [phase-2, frontend, merchants, onboarding]

problem: "No UI to add new merchants. Users must use CLI or direct DB manipulation."
desired_outcome: "/merchants/new shows a form where users enter a store URL. Platform auto-detection happens client-side. On submit, merchant is created and crawl triggered. User redirected to merchant detail."

scope_in:
  - "URL input field with validation"
  - "Platform auto-detection heuristic (shopify.com domains, /shop/ URL patterns)"
  - "Preview: extracts canonical_domain, suggested name from URL"
  - "Submit: POST /api/v1/merchants then POST /api/v1/merchants/{id}/crawl"
  - "Redirect to /merchants/:id after creation"
  - "useCreateMerchant() mutation hook"
scope_out:
  - "WooCommerce or agentic-specific configuration (just platform_type selection)"
  - "Bulk import"

dependencies:
  blockers: [SC-12, SC-17, SC-22]
  unblocks: []

acceptance_criteria:
  - id: AC-1
    text: "Form renders at /merchants/new"
    verify: "manual: navigate to /merchants/new, form visible"
    status: pending
  - id: AC-2
    text: "Entering a URL auto-fills canonical_domain and detects platform"
    verify: "manual: type 'https://somestore.myshopify.com', platform shows 'Shopify'"
    status: pending
  - id: AC-3
    text: "Submit creates merchant and triggers crawl, redirects to detail"
    verify: "manual: submit form, verify redirect to /merchants/:id"
    status: pending
  - id: AC-4
    text: "TypeScript compilation passes"
    verify: "cd frontend && npx tsc --noEmit"
    status: pending

verification_required:
  - "cd frontend && npx tsc --noEmit"
  - "cd frontend && npm run build"

context_refs:
  - "/home/h6nk/SzimplaCoffee/.plans/SC-24-execution-plan.md"

slices:
  - id: S1
    title: "Add merchant form + useCreateMerchant hook"
    status: pending
    depends_on: []
    files_create:
      - "frontend/src/routes/merchants.new.tsx"
      - "frontend/src/hooks/useCreateMerchant.ts"
      - "frontend/src/lib/platform-detect.ts"
    files_read_only:
      - "frontend/src/api/client.ts"
      - "frontend/src/components/ui/input.tsx"
      - "frontend/src/components/ui/button.tsx"
      - "frontend/src/components/ui/select.tsx"
    checks:
      - "cd frontend && npx tsc --noEmit"
    evidence: []
    notes: "platform-detect.ts: pure function, testable, no API call needed"

delivery:
  branch: "feat/SC-24-add-merchant"
  commits: []
  pr_url: ""
  started_at: ""
  completed_at: ""
  verification:
    required_passed: false
    commands_run: []
    evidence_refs: []

notes:
  - "Platform detection heuristics: .myshopify.com = shopify, woocommerce plugin patterns = woocommerce"
  - "canonical_domain = hostname extracted from URL"
  - "Default name = hostname without www. prefix"
