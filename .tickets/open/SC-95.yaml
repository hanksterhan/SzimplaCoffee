id: SC-95
title: "Improve crawl quality scoring and enforce low-CPU serialized crawl execution"
type: feature
priority: p1
status: done
owner: h6nk-bot
created_at: "2026-03-19T18:46:05Z"
updated_at: "2026-03-19T20:15:13Z"
labels: ["crawl", "quality", "scheduler", "performance"]

problem: "Merchant crawl quality is not yet observable enough to separate trusted merchants from degraded ones, and we explicitly want to avoid overloading any session or the CPU during normal automation. Crawl work needs clearer quality signals and conservative execution behavior."
desired_outcome: "SzimplaCoffee records crawl-quality/reliability signals per merchant and runs routine crawl work in serialized or staggered low-CPU patterns, so crawl quality improves without stressing the machine."

scope_in:
  - "Audit current crawl scheduling/execution behavior against the new low-CPU guardrail"
  - "Add or improve per-merchant crawl quality/reliability signals"
  - "Ensure routine crawl execution prefers serialized/staggered work instead of parallel heavy jobs"
  - "Expose enough quality signal to support merchant trust decisions in later work"
scope_out:
  - "Aggressive merchant expansion"
  - "Agentic fallback overhaul for every crawler"
  - "Full UI redesign of crawl monitoring"

dependencies:
  blockers: ["SC-93", "SC-94"]
  unblocks: ["SC-96"]

acceptance_criteria:
  - id: AC-1
    text: "Per-merchant crawl quality/reliability signals are persisted or computed in a stable way"
    verify: "Run targeted backend verification showing quality/trust-relevant crawl fields for representative merchants"
    status: passed
  - id: AC-2
    text: "Routine crawl execution follows serialized or staggered low-CPU behavior"
    verify: "Inspect scheduler/crawl execution path and run a bounded verification proving heavy jobs are not launched in parallel"
    status: pending
  - id: AC-3
    text: "Touched crawl/scheduler tests pass"
    verify: "Run targeted backend tests for crawl/scheduler behavior"
    status: pending
  - id: AC-4
    text: "Backend test suite remains green if shared contracts changed"
    verify: "cd backend && .venv/bin/pytest tests/ -q"
    status: pending

verification_required:
  - "Targeted crawl/scheduler verification"
  - "cd backend && .venv/bin/pytest tests/ -q"

context_refs:
  - ".plans/SC-95-execution-plan.md"
  - "autopilot/policy.yaml"

slices:
  - id: S1
    title: "Add crawl quality/reliability signals"
    status: pending
    depends_on: []
    files_create: []
    files_modify:
      - "backend/src/szimplacoffee/services/"
      - "backend/src/szimplacoffee/models.py"
      - "backend/tests/"
    files_read_only: []
    checks:
      - "Quality signal supports merchant trust decisions"
    evidence: []
    notes: "Use the smallest set of signals that are actually observable."
  - id: S2
    title: "Constrain crawl execution to low-CPU serialized/staggered patterns"
    status: pending
    depends_on: ["S1"]
    files_create: []
    files_modify:
      - "backend/src/szimplacoffee/services/scheduler.py"
      - "backend/src/szimplacoffee/services/"
      - "backend/tests/"
    files_read_only:
      - "autopilot/policy.yaml"
    checks:
      - "No routine parallel heavy crawl fan-out"
      - "Execution still makes progress"
    evidence: []
    notes: "Guardrail: do not overload any session or the CPU."

delivery:
  branch: ""
  commits: []
  pr_url: ""
  started_at: ""
  completed_at: ""
  verification:
    required_passed: false
    commands_run: []
    evidence_refs: []

notes:
  - "This ticket turns the CPU/session guardrail into enforceable implementation behavior."
