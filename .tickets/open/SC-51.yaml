id: "SC-51"
title: "Refactor crawling into layered strategies with crawl-quality scoring"
type: "feature"
priority: "p1"
status: "draft"
owner: "h6nk-bot"
created_at: "2026-03-15T07:00:00Z"
updated_at: "2026-03-15T07:00:00Z"
labels:
  - "feature"
  - "integration"
  - "crawling"
  - "reliability"
problem: "Discovery and platform detection are brittle for large-scale merchant coverage, and the crawler stack does not expose strategy quality clearly enough to know which merchants are trustworthy."
desired_outcome: "Crawling follows a layered extraction strategy with per-merchant strategy records and measurable crawl quality, so high-value merchants can be supported deliberately and low-confidence merchants are visibly flagged."
scope_in:
  - "Crawler strategy selection is explicit and ordered across feed, structured page data, DOM extraction, and agentic fallback layers."
  - "Merchant-level crawl strategy and quality metrics are persisted or surfaced consistently enough to identify low-confidence merchants."
  - "Top merchants can be assessed for crawl reliability without ad hoc log inspection."
scope_out:
  - "Follow-on implementation details outside this ticket's described slices."
dependencies:
  blockers:
    - "SC-50"
  unblocks:
    - "SC-52"
    - "SC-53"
acceptance_criteria:
  -
    id: "AC-1"
    text: "Crawler strategy selection is explicit and ordered across feed, structured page data, DOM extraction, and agentic fallback layers."
    verify: "cd backend && . .venv/bin/activate && pytest -q"
    status: "pending"
  -
    id: "AC-2"
    text: "Merchant-level crawl strategy and quality metrics are persisted or surfaced consistently enough to identify low-confidence merchants."
    verify: "cd backend && . .venv/bin/activate && pytest -q"
    status: "pending"
  -
    id: "AC-3"
    text: "Top merchants can be assessed for crawl reliability without ad hoc log inspection."
    verify: "cd backend && . .venv/bin/activate && pytest -q"
    status: "pending"
verification_required:
  - "cd backend && . .venv/bin/activate && pytest -q"
  - "cd backend && . .venv/bin/activate && ruff check src tests"
  - "bash -lc \"test -f backend/src/szimplacoffee/services/crawlers.py && echo OK\""
context_refs:
  - ".plans/SC-51-execution-plan.md"
  - ".plans/next-steps-march15.md"
slices:
  -
    id: "S1"
    title: "Make crawl strategy layers explicit"
    status: "pending"
    depends_on: []
    files_create: []
    files_modify:
      - "backend/src/szimplacoffee/services/crawlers.py"
      - "backend/src/szimplacoffee/services/platforms.py"
    files_read_only:
      - "backend/src/szimplacoffee/models.py"
    checks:
      - "cd backend && . .venv/bin/activate && pytest -q"
    evidence: []
    notes: "Agentic fallback must remain a last resort with provenance."
  -
    id: "S2"
    title: "Persist crawl-quality metrics and merchant strategy state"
    status: "pending"
    depends_on:
      - "S1"
    files_create: []
    files_modify:
      - "backend/src/szimplacoffee/models.py"
      - "backend/src/szimplacoffee/api/merchants.py"
      - "backend/src/szimplacoffee/services/crawlers.py"
    files_read_only:
      - "backend/src/szimplacoffee/services/discovery.py"
    checks:
      - "cd backend && . .venv/bin/activate && pytest -q"
    evidence: []
    notes: "Quality metrics should support later dashboarding and watchlist thresholds."
delivery:
  branch: "feat/SC-51-crawl-strategy-quality-layer"
  commits: []
  pr_url: ""
  started_at: ""
  completed_at: ""
  verification:
    required_passed: false
    commands_run: []
    evidence_refs: []
notes:
  - "Generated from .plans/next-steps-march15.md in strict-schema migration batch."
