id: SC-75
title: "Crawl batch 2 — Blue Bottle, Stumptown, Heart, George Howell, Passenger"
type: task
priority: p1
status: ready
owner: h6nk-bot
created_at: "2026-03-18T05:50:00Z"
updated_at: "2026-03-18T05:50:00Z"
labels: ["merchant-coverage", "epic-1", "crawling"]

problem: "Merchants 8-12 (Blue Bottle, Stumptown, Heart, George Howell, Passenger) have no product data yet."
desired_outcome: "Merchants 8-12 have been crawled. crawl_runs records exist. Any crawl failures are documented."

scope_in:
  - "Crawl merchants with IDs 8, 9, 10, 11, 12 only"
  - "Verify crawl_runs rows created for each"
  - "Log product count before and after"
  - "Document failures with error reason"
scope_out:
  - "Merchants 3-7 (SC-58) and 13-15 (SC-76)"
  - "Fixing broken adapters"
  - "Metadata parsing"

dependencies:
  blockers: ["SC-58"]
  unblocks: ["SC-76"]

acceptance_criteria:
  - id: AC-1
    text: "crawl_runs has at least one row for each of merchants 8-12"
    verify: "cd backend && .venv/bin/python -c \"from szimplacoffee.db import engine; from sqlalchemy import text; print(engine.connect().execute(text('SELECT merchant_id, status FROM crawl_runs WHERE merchant_id BETWEEN 8 AND 12')).fetchall())\""
    status: pending
  - id: AC-2
    text: "products table has more rows after crawl than after SC-58"
    verify: "cd backend && .venv/bin/python -c \"from szimplacoffee.db import engine; from sqlalchemy import text; print(engine.connect().execute(text('SELECT COUNT(*) FROM products')).scalar())\""
    status: pending

verification_required:
  - "cd backend && .venv/bin/pytest tests/ -q"
  - "cd backend && .venv/bin/ruff check src/ tests/"

context_refs:
  - ".plans/SC-75-execution-plan.md"

slices:
  - id: S1
    title: "Crawl merchants 8-12 and record results"
    status: pending
    depends_on: []
    files_create: []
    files_modify: []
    files_read_only:
      - "backend/src/szimplacoffee/services/crawlers.py"
      - "backend/src/szimplacoffee/cli.py"
    checks:
      - "crawl_runs populated for merchants 8-12"
    evidence: []
    notes: "Blue Bottle uses custom platform — may fail adapter detection. Document result."

delivery:
  branch: ""
  commits: []
  pr_url: ""
  started_at: ""
  completed_at: ""
  verification:
    required_passed: false
    commands_run: []
    evidence_refs: []

notes:
  - "Batch 2 of 3 merchant crawl splits. Batch 1: SC-58. Batch 3: SC-76."
