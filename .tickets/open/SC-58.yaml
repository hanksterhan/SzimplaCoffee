id: SC-58
title: "Run crawls on all newly imported merchants and verify product ingestion"
type: task
priority: p1
status: ready
owner: h6nk-bot
created_at: "2026-03-18T00:00:00Z"
updated_at: "2026-03-18T00:00:00Z"
labels: ["merchant-coverage", "epic-1", "crawling"]

problem: "Merchants imported in SC-57 have no product data. Without crawling them we cannot generate recommendations or validate crawl adapter quality."
desired_outcome: "All newly imported merchants have been crawled at least once. The products table grows and crawl_runs records show success or failure per merchant."

scope_in:
  - "Run szimpla crawl-all to trigger crawls on all active merchants"
  - "Verify products table row count increases after crawl"
  - "Check crawl_runs for each new merchant — success vs failure"
  - "Log failing merchants with error reason"
scope_out:
  - "Fixing broken adapters (tracked separately)"
  - "Metadata parsing (SC-61 to SC-63)"
  - "Trust tier promotion (SC-60)"

dependencies:
  blockers: ["SC-57"]
  unblocks: ["SC-60", "SC-61"]

acceptance_criteria:
  - id: AC-1
    text: "products table has more rows after crawl-all than before"
    verify: "cd backend && python -c \"from szimplacoffee.db import engine; from sqlalchemy import text; print(engine.connect().execute(text('SELECT COUNT(*) FROM products')).scalar())\""
    status: pending
  - id: AC-2
    text: "crawl_runs table has at least one row per newly imported merchant"
    verify: "cd backend && python -c \"from szimplacoffee.db import engine; from sqlalchemy import text; print(engine.connect().execute(text('SELECT merchant_id, status, COUNT(*) FROM crawl_runs GROUP BY merchant_id, status')).fetchall())\""
    status: pending
  - id: AC-3
    text: "At least 50% of newly imported merchants produce at least 1 product"
    verify: "cd backend && python -c \"from szimplacoffee.db import engine; from sqlalchemy import text; print(engine.connect().execute(text('SELECT COUNT(DISTINCT merchant_id) FROM products')).scalar())\""
    status: pending

verification_required:
  - "pytest tests/ -v"
  - "ruff check src/ tests/"

context_refs:
  - "/home/h6nk/SzimplaCoffee/.plans/SC-58-execution-plan.md"

slices:
  - id: S1
    title: "Run crawl-all and capture output"
    status: pending
    depends_on: []
    files_create: []
    files_modify: []
    files_read_only:
      - "backend/src/szimplacoffee/services/crawlers.py"
    checks:
      - "szimpla crawl-all exits without unhandled exception"
    evidence: []
    notes: "Capture stdout/stderr to log for analysis"
  - id: S2
    title: "Verify product and crawl_run row counts"
    status: pending
    depends_on: ["S1"]
    files_create: []
    files_modify:
      - "backend/tests/"
    files_read_only: []
    checks:
      - "products count > pre-crawl baseline"
      - "crawl_runs has rows for new merchants"
    evidence: []
    notes: "Write a quick pytest or inline assertion to document the result"

delivery:
  branch: ""
  commits: []
  pr_url: ""
  started_at: ""
  completed_at: ""
  verification:
    required_passed: false
    commands_run: []
    evidence_refs: []

notes:
  - "Depends on SC-57 completing first"
  - "Failures are expected — goal is to document the baseline, not achieve 100% success"
