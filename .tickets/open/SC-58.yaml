id: SC-58
title: "Crawl batch 1 — Tier A anchors (Onyx, Intelligentsia, Counter Culture, Verve, Sightglass)"
type: task
priority: p1
status: ready
owner: h6nk-bot
created_at: "2026-03-18T00:00:00Z"
updated_at: "2026-03-18T05:50:00Z"
labels: ["merchant-coverage", "epic-1", "crawling"]

problem: "5 high-quality Tier A merchants were imported in SC-57 but have no product data yet. These are the best candidates for recommendations and should be crawled first."
desired_outcome: "Merchants 3-7 (Onyx, Intelligentsia, Counter Culture, Verve, Sightglass) have been crawled. crawl_runs records exist for each. products table grows."

scope_in:
  - "Crawl merchants with IDs 3, 4, 5, 6, 7 only"
  - "Verify crawl_runs rows created for each"
  - "Log product count before and after"
  - "Document failures with error reason"
scope_out:
  - "Merchants 8-15 (handled in SC-75 and SC-76)"
  - "Fixing broken adapters"
  - "Metadata parsing (SC-61 to SC-63)"

dependencies:
  blockers: ["SC-57"]
  unblocks: ["SC-75"]

acceptance_criteria:
  - id: AC-1
    text: "crawl_runs has at least one row for each of merchants 3-7"
    verify: "cd backend && .venv/bin/python -c \"from szimplacoffee.db import engine; from sqlalchemy import text; print(engine.connect().execute(text('SELECT merchant_id, status FROM crawl_runs WHERE merchant_id BETWEEN 3 AND 7')).fetchall())\""
    status: pending
  - id: AC-2
    text: "products table has more rows after crawl than before"
    verify: "cd backend && .venv/bin/python -c \"from szimplacoffee.db import engine; from sqlalchemy import text; print(engine.connect().execute(text('SELECT COUNT(*) FROM products')).scalar())\""
    status: pending

verification_required:
  - "cd backend && .venv/bin/pytest tests/ -q"
  - "cd backend && .venv/bin/ruff check src/ tests/"

context_refs:
  - ".plans/SC-58-execution-plan.md"

slices:
  - id: S1
    title: "Crawl merchants 3-7 and record results"
    status: pending
    depends_on: []
    files_create: []
    files_modify: []
    files_read_only:
      - "backend/src/szimplacoffee/services/crawlers.py"
      - "backend/src/szimplacoffee/cli.py"
    checks:
      - "szimpla crawl-all exits without unhandled exception"
    evidence: []
    notes: "Use szimpla crawl-all or individual szimpla add-merchant --crawl-only per merchant ID"

delivery:
  branch: ""
  commits: []
  pr_url: ""
  started_at: ""
  completed_at: ""
  verification:
    required_passed: false
    commands_run: []
    evidence_refs: []

notes:
  - "Split from original SC-58 (all-15 crawl) to limit each run to 5 merchants"
  - "Batch 2: SC-75 (merchants 8-11). Batch 3: SC-76 (merchants 12-15)"
