id: SC-64
title: "Add metadata fill-rate metrics to /api/v1/dashboard endpoint"
type: feature
priority: p2
status: ready
owner: h6nk-bot
created_at: "2026-03-18T00:00:00Z"
updated_at: "2026-03-18T00:00:00Z"
labels: ["metadata-pipeline", "epic-2", "dashboard"]

problem: "The dashboard gives no visibility into coffee metadata quality. It is impossible to monitor fill rate progress or regression without querying the DB directly."
desired_outcome: "The /api/v1/dashboard response includes total_products, products_with_origin, products_with_process, and products_with_roast_level counts. The Today view renders these metrics."

scope_in:
  - "Add metadata fill-rate counts to dashboard.py API handler"
  - "Update dashboard Pydantic schema to include the new fields"
  - "Update frontend index.tsx Today view to display fill rate percentages"
  - "Regenerate API types if schema changes"
scope_out:
  - "Historical fill rate trending or charting"
  - "Per-merchant metadata breakdown"
  - "Alerts or thresholds on fill rate"

dependencies:
  blockers: ["SC-61"]
  unblocks: []

acceptance_criteria:
  - id: AC-1
    text: "GET /api/v1/dashboard returns total_products, products_with_origin, products_with_process, products_with_roast_level"
    verify: "curl -s http://localhost:8000/api/v1/dashboard | python -c \"import json,sys; d=json.load(sys.stdin); print(d.get('total_products'), d.get('products_with_origin'), d.get('products_with_process'), d.get('products_with_roast_level'))\""
    status: pending
  - id: AC-2
    text: "Frontend Today view displays fill rate percentages without console errors"
    verify: "cd frontend && npm run build 2>&1 | tail -5"
    status: pending
  - id: AC-3
    text: "ruff check and pytest pass"
    verify: "cd backend && ruff check src/ tests/ && pytest tests/ -v"
    status: pending

verification_required:
  - "ruff check src/ tests/"
  - "pytest tests/ -v"
  - "cd frontend && npm run build"

context_refs:
  - "/home/h6nk/SzimplaCoffee/.plans/SC-64-execution-plan.md"

slices:
  - id: S1
    title: "Add fill-rate fields to dashboard API and schema"
    status: pending
    depends_on: []
    files_create: []
    files_modify:
      - "backend/src/szimplacoffee/api/dashboard.py"
    files_read_only: []
    checks:
      - "curl /api/v1/dashboard shows new fields"
    evidence: []
    notes: "Use COUNT + WHERE IS NOT NULL SQL queries"
  - id: S2
    title: "Update frontend Today view to show fill rate"
    status: pending
    depends_on: ["S1"]
    files_create: []
    files_modify:
      - "frontend/src/routes/index.tsx"
    files_read_only: []
    checks:
      - "npm run build passes"
      - "fill rate percentages visible in UI"
    evidence: []
    notes: "Show as: 'Origin: 47% | Process: 38% | Roast: 52%'"

delivery:
  branch: ""
  commits: []
  pr_url: ""
  started_at: ""
  completed_at: ""
  verification:
    required_passed: false
    commands_run: []
    evidence_refs: []

notes:
  - "Run npm run gen:api after backend schema change if using generated types"
