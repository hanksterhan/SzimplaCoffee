id: SC-111
title: "Merchant expansion batch 3 (10 new roasters)"
type: feature
priority: p1
status: ready
owner: h6nk-bot
created_at: "2026-03-20T12:06:00Z"
updated_at: "2026-03-20T12:06:00Z"
labels: ["backend", "merchants", "crawl", "expansion", "phase-4"]

problem: >
  SzimplaCoffee has 36 active merchants. The seed list contains ~14 more high-quality
  Tier A/B candidates not yet imported. Broader merchant coverage improves daily deal
  discovery and recommendation diversity.

desired_outcome: >
  10 new merchants from the priority seed list are imported via `szimpla add-merchant`.
  Each is crawled serially (not in parallel) to avoid CPU overload. Final active merchant
  count reaches 44–46. Any merchants that fail initial crawl are noted but do not block
  the ticket.

scope_in:
  - "Import ~10 high-priority merchants from seed list using `szimpla add-merchant <url>`"
  - "Serial execution (one at a time), not parallel"
  - "Verify crawl produces products for each successful import"
  - "Report success/failure counts in delivery notes"

scope_out:
  - "No changes to crawl logic or adapter code"
  - "No new scrapers or platform support"
  - "No frontend changes"

dependencies:
  blockers: []
  unblocks: ["SC-116"]

acceptance_criteria:
  - id: AC-1
    text: "At least 8 of 10 targeted merchants successfully imported with products"
    verify: "cd backend && . .venv/bin/activate && szimpla crawl-all | head -20 && python3 -c \"from szimplacoffee.db import get_session; from szimplacoffee.models import Merchant; s=next(get_session()); print(s.query(Merchant).filter(Merchant.is_active==True).count(), 'active merchants')\""
    status: pending
  - id: AC-2
    text: "Total active merchant count ≥ 44"
    verify: "cd backend && . .venv/bin/activate && python3 -c \"from szimplacoffee.db import get_session; from szimplacoffee.models import Merchant; s=next(get_session()); print(s.query(Merchant).filter(Merchant.is_active==True).count())\""
    status: pending
  - id: AC-3
    text: "Backend tests pass"
    verify: "cd backend && pytest tests/ -q"
    status: pending

verification_required:
  - "cd backend && pytest tests/ -q"
  - "cd backend && ~/.local/bin/ruff check src/ tests/"

context_refs:
  - "/home/h6nk/SzimplaCoffee/.plans/SC-111-execution-plan.md"
  - "/home/h6nk/SzimplaCoffee/SzimplaCoffee/brain/merchants/top-500-seed.md"
  - "/home/h6nk/SzimplaCoffee/backend/src/szimplacoffee/cli.py"

slices:
  - id: S1
    title: "Identify and import 10 priority merchants serially"
    status: pending
    depends_on: []
    files_create: []
    files_modify: []
    files_read_only:
      - "SzimplaCoffee/brain/merchants/top-500-seed.md"
    checks:
      - "cd backend && . .venv/bin/activate && python3 -c \"from szimplacoffee.db import get_session; from szimplacoffee.models import Merchant; s=next(get_session()); print(s.query(Merchant).filter(Merchant.is_active==True).count())\""
    evidence: []
    notes: >
      Targets (verify not already imported): Olympia Coffee, Camber Coffee, Ritual Coffee,
      Broadsheet Coffee, Bird Rock Coffee, Equator Coffees, Greater Goods Coffee,
      Temple Coffee, Huckleberry Roasters, Methodical Coffee.
      Use: szimpla add-merchant <url> for each. Serial only.

delivery:
  branch: ""
  commits: []
  pr_url: ""
  started_at: ""
  completed_at: ""
  verification:
    required_passed: false
    commands_run: []
    evidence_refs: []

notes:
  - "Serial crawl is required per policy: avoid_cpu_overload=true"
  - "Some merchants may fail crawl — accept ≥8/10 success rate"
  - "Seed list is at SzimplaCoffee/brain/merchants/top-500-seed.md"
