acceptance_criteria:
- id: AC-1
  status: passed
  text: szimpla import-merchants --file test_merchants.txt imports all valid URLs
    and prints a summary
  verify: echo 'https://bluebottlecoffee.com' > /tmp/test_merchants.txt && cd backend
    && . .venv/bin/activate && szimpla import-merchants --file /tmp/test_merchants.txt
- id: AC-2
  status: passed
  text: Duplicate URLs are skipped without error
  verify: cd backend && . .venv/bin/activate && szimpla import-merchants --file /tmp/test_merchants.txt
    2>&1 | grep -i 'skip\|already\|duplicate'
- id: AC-3
  status: passed
  text: Invalid URLs are reported in the failure summary without crashing
  verify: echo 'not-a-url' > /tmp/bad_merchants.txt && cd backend && . .venv/bin/activate
    && szimpla import-merchants --file /tmp/bad_merchants.txt 2>&1 | grep -i 'fail\|error\|invalid'
context_refs:
- /home/h6nk/SzimplaCoffee/.plans/SC-59-execution-plan.md
created_at: '2026-03-18T00:00:00Z'
delivery:
  branch: ''
  commits:
  - cdad418
  completed_at: '2026-03-18T11:10:00Z'
  pr_url: ''
  started_at: '2026-03-18T11:02:00Z'
  verification:
    commands_run:
    - ruff check src/szimplacoffee/cli.py
    - 'pytest tests/ -q  # 129 passed'
    evidence_refs: []
    required_passed: true
dependencies:
  blockers: []
  unblocks:
  - SC-57
desired_outcome: A new CLI command szimpla import-merchants --file merchants.txt reads
  a list of URLs, attempts to import each, and prints a per-URL success/failure summary.
id: SC-59
labels:
- merchant-coverage
- epic-1
- cli
notes:
- 'Newline-delimited URL file format; ignore blank lines and # comments'
owner: h6nk-bot
priority: p2
problem: Adding merchants one at a time via szimpla add-merchant is tedious. There
  is no way to import a batch of URLs from a file, making bulk seed-list ingestion
  slow and error-prone.
scope_in:
- Add import-merchants CLI command to cli.py
- Accept --file path argument pointing to a newline-delimited URL list
- Call existing add-merchant logic per URL
- Print success/failure summary with counts
- Skip duplicate URLs already in registry
scope_out:
- Crawling imported merchants (handled by crawl-all)
- GUI/web interface for bulk import
- Async/parallel import
slices:
- checks:
  - szimpla import-merchants --help shows usage
  - command reads file and iterates URLs
  depends_on: []
  evidence: []
  files_create: []
  files_modify:
  - backend/src/szimplacoffee/cli.py
  - backend/src/szimplacoffee/bootstrap.py
  files_read_only: []
  id: S1
  notes: Reuse existing add_merchant() helper; wrap with try/except per URL
  status: pending
  title: Add import-merchants command to cli.py
status: done
title: Add bulk merchant import CLI command with URL list file support
type: feature
updated_at: '2026-03-18T11:10:00Z'
verification_required:
- ruff check src/ tests/
- pytest tests/ -v
