id: "SC-44"
title: "Stabilize backend tests and clean package boundaries"
type: "bugfix"
priority: "p1"
status: "done"
owner: "h6nk-bot"
created_at: "2026-03-15T07:00:00Z"
updated_at: "2026-03-15T18:34:02Z"
labels:
  - "bugfix"
  - "unit"
  - "stability"
  - "backend"
  - "repo-hygiene"
problem: "The backend regression suite currently fails during collection and stale package artifacts blur the actual runtime boundary. This blocks safe iteration on every other item in the March 15 plan."
desired_outcome: "The backend test suite runs cleanly against the current package layout, stale package confusion is removed or quarantined, and the repo has one obvious backend package root."
scope_in:
  - "Backend tests collect and run without import errors against the current package structure."
  - "Stale package artifacts are removed or isolated so there is one clear backend package root."
  - "Repo guidance identifies the canonical backend package root for future contributors."
scope_out:
  - "Follow-on implementation details outside this ticket's described slices."
dependencies:
  blockers: []
  unblocks:
    - "SC-45"
    - "SC-46"
    - "SC-48"
    - "SC-49"
    - "SC-50"
    - "SC-51"
    - "SC-52"
    - "SC-53"
acceptance_criteria:
  -
    id: "AC-1"
    text: "Backend tests collect and run without import errors against the current package structure."
    verify: "cd backend && . .venv/bin/activate && pytest -q"
    status: "passed"
  -
    id: "AC-2"
    text: "Stale package artifacts are removed or isolated so there is one clear backend package root."
    verify: "bash -lc \"test -d backend/src/szimplacoffee && echo OK\""
    status: "passed"
  -
    id: "AC-3"
    text: "Repo guidance identifies the canonical backend package root for future contributors."
    verify: "bash -lc \"test -f AGENTS.md && test -f CLAUDE.md && echo OK\""
    status: "passed"
verification_required:
  - "cd backend && . .venv/bin/activate && pytest -q"
  - "cd backend && . .venv/bin/activate && ruff check src tests"
  - "bash -lc \"test -d backend/src/szimplacoffee && echo OK\""
context_refs:
  - ".plans/SC-44-execution-plan.md"
  - ".plans/next-steps-march15.md"
slices:
  -
    id: "S1"
    title: "Repair broken backend test imports"
    status: "complete"
    depends_on: []
    files_create: []
    files_modify:
      - "backend/tests/test_recommendations.py"
      - "backend/tests/"
    files_read_only:
      - "backend/src/szimplacoffee/main.py"
      - "backend/src/szimplacoffee/"
    checks:
      - "cd backend && . .venv/bin/activate && pytest -q"
    evidence:
      - "Updated backend/tests/test_recommendations.py to use current recommendation-service helpers instead of removed main.py display helpers."
      - "Verified with cd backend && ../.venv/bin/pytest -q -> 41 passed."
    notes: "Update tests to current runtime modules instead of resurrecting removed helpers."
  -
    id: "S2"
    title: "Quarantine or remove stale top-level package artifacts"
    status: "complete"
    depends_on:
      - "S1"
    files_create: []
    files_modify:
      - "src/"
      - "README.md"
      - "AGENTS.md"
      - "CLAUDE.md"
    files_read_only:
      - "backend/src/szimplacoffee/"
    checks:
      - "bash -lc \"test -d backend/src/szimplacoffee && echo OK\""
    evidence:
      - "Moved the stray repository-root src directory out of the repo to /tmp/claude/SC-44/repo-root-src-artifacts."
      - "Added /src/ to .gitignore and documented backend/src/szimplacoffee as the canonical backend package root in README.md, AGENTS.md, and CLAUDE.md."
    notes: "Preserve active entrypoints while cleaning package-boundary confusion."
delivery:
  branch: "codex/sc-44-stabilize-backend-tests-and-clean-package-boundaries"
  commits:
    - "73c8e238e10257b6d0a1fad9050e876983c5420a"
  pr_url: "https://github.com/hanksterhan/SzimplaCoffee/pull/1"
  started_at: "2026-03-15T18:22:34Z"
  completed_at: "2026-03-15T18:34:02Z"
  verification:
    required_passed: true
    commands_run:
      - "cd backend && ../.venv/bin/pytest -q"
      - "cd backend && ../.venv/bin/ruff check src tests"
      - "bash -lc \"find . -maxdepth 2 -type d \\( -name src -o -path './backend/src' \\) | sort\""
      - "bash -lc \"rg -n 'canonical backend package root|one obvious backend package root' AGENTS.md CLAUDE.md README.md -S\""
    evidence_refs:
      - "pytest: 41 passed in 0.41s"
      - "ruff: All checks passed!"
      - "package roots: ./backend/src and ./frontend/src only"
notes:
  - "Generated from .plans/next-steps-march15.md in strict-schema migration batch."
