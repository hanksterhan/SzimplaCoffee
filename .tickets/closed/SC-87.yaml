id: SC-87
title: "Improve variety extraction to reach ≥50% fill rate"
type: task
priority: p2
status: done
owner: h6nk-bot
created_at: "2026-03-18T20:17:00Z"
updated_at: "2026-03-19T04:10:00Z"
labels: ["metadata", "coffee-parser", "backend"]

problem: "Variety fill rate is 26% (234/890 products). Specialty roasters routinely list cultivar names in product descriptions (Gesha, Bourbon, Caturra, SL28, Typica, Pacamara, Catuai, etc.) but the coffee_parser misses many of these."
desired_outcome: "Variety fill rate reaches ≥50% after backfill. Common cultivar names and their alternate spellings are matched by the parser."

scope_in:
  - "Audit current variety patterns in coffee_parser.py"
  - "Add patterns for: Gesha/Geisha, Bourbon, Caturra, SL28, SL34, Typica, Pacamara, Catuai, Heirloom, Wush Wush, Marsellesa, Pink Bourbon"
  - "Run szimpla backfill-metadata and verify fill rate"
  - "Add or update tests for new variety patterns"
scope_out:
  - "Origin or process extraction (SC-85)"
  - "Frontend changes"

dependencies:
  blockers: ["SC-85"]
  unblocks: []

acceptance_criteria:
  - id: AC-1
    text: "Variety fill rate reaches ≥50% after backfill"
    verify: "cd backend && .venv/bin/python -c \"import sys; sys.path.insert(0,'src'); from szimplacoffee.db import engine; from sqlalchemy import text; c=engine.connect(); t=c.execute(text('SELECT COUNT(*) FROM products')).scalar(); f=c.execute(text(\\\"SELECT COUNT(*) FROM products WHERE variety_text IS NOT NULL AND variety_text != ''\\\")).scalar(); print(f'{f}/{t} = {round(100*f/max(t,1))}%')\""
    status: passed
  - id: AC-2
    text: "Backend tests pass (≥84 green)"
    verify: "cd backend && .venv/bin/pytest tests/ -q"
    status: passed

verification_required:
  - "cd backend && .venv/bin/pytest tests/ -q"

context_refs:
  - ".plans/SC-87-execution-plan.md"

slices:
  - id: S1
    title: "Add variety cultivar patterns to coffee_parser"
    status: done
    depends_on: []
    files_create: []
    files_modify:
      - "backend/src/szimplacoffee/services/coffee_parser.py"
      - "backend/tests/test_coffee_parser.py"
      - "backend/src/szimplacoffee/cli.py"
    files_read_only:
      - "backend/src/szimplacoffee/models.py"
    checks:
      - "All listed cultivar names match via parser"
      - "pytest passes"
    evidence:
      - "459/891 = 52% variety fill rate after backfill"
      - "221 tests passing"
    notes: "Patterns case-insensitive with word-boundary anchors. Added country defaults for Ethiopia, Rwanda, Burundi, Kenya, Uganda, Bolivia, Costa Rica, Peru."

delivery:
  branch: "main"
  commits:
    - "b84c5db SC-87: Improve variety extraction to reach ≥50% fill rate"
  pr_url: ""
  started_at: "2026-03-19T03:00:00Z"
  completed_at: "2026-03-19T04:10:00Z"
  verification:
    required_passed: true
    commands_run:
      - "cd backend && .venv/bin/pytest tests/ -q → 221 passed"
      - "variety fill rate: 459/891 = 52%"
    evidence_refs:
      - "459/891 = 52% variety fill rate (before: 235/891 = 26%)"
      - "18 new explicit variety keywords added"
      - "7 regex normalization patterns added (SL-28→SL28, accented variants)"
      - "8 country-level variety defaults added"
      - "29 new tests added for SC-87"
      - "221 total backend tests passing"

notes:
  - "Strategy: new explicit keywords + regex normalization patterns + country-level defaults."
  - "Country defaults grounded in specialty coffee knowledge (Ethiopia→Heirloom, Rwanda→Bourbon, etc.)."
  - "Backfill also uses stored origin_country for products whose name lacks country mention."
  - "Gesha / Geisha are the same cultivar with alternate spellings — both kept as valid."
