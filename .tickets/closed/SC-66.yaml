id: SC-66
title: "Add recommendation engine regression test with seeded offer_snapshot data"
type: task
priority: p1
status: done
owner: h6nk-bot
created_at: "2026-03-18T00:00:00Z"
updated_at: "2026-03-18T00:00:00Z"
labels: ["recommendation", "epic-3", "bug"]

problem: "No regression test exists that seeds a merchant, product, variant, and offer_snapshot, then verifies the recommendation engine returns at least one ranked result. Without this, silent regressions in the candidate pipeline will go undetected."
desired_outcome: "A pytest test seeds the minimum required data and asserts that build_recommendations returns at least 1 candidate with a valid score. This guards the full pipeline from breakage."

scope_in:
  - "Write pytest test that seeds merchant + quality_profile + product + variant + offer_snapshot"
  - "Assert build_recommendations returns >= 1 candidate with score > 0"
  - "Assert wait_recommendation is False for seeded data with inventory=0"
scope_out:
  - "Scoring algorithm changes"
  - "Frontend changes"
  - "Explain mode (SC-67)"

dependencies:
  blockers: []
  unblocks: ["SC-67"]

acceptance_criteria:
  - id: AC-1
    text: "POST /api/v1/recommendations returns at least 1 ranked result when products and offer_snapshots exist"
    verify: "curl -s -X POST http://localhost:8000/api/v1/recommendations -H 'Content-Type: application/json' -d '{\"current_inventory_grams\": 0, \"style\": \"pourover\"}' | python -c \"import json,sys; d=json.load(sys.stdin); results=d.get('results', d.get('recommendations', [])); print(len(results), 'results')\""
    status: pending
  - id: AC-2
    text: "pytest test for recommendation with seeded offer_snapshot data passes"
    verify: "cd backend && pytest tests/ -v -k recommendation"
    status: pending
  - id: AC-3
    text: "ruff check passes on modified files"
    verify: "cd backend && ruff check src/szimplacoffee/services/recommendations.py"
    status: pending

verification_required:
  - "ruff check src/ tests/"
  - "pytest tests/ -v"

context_refs:
  - "/home/h6nk/SzimplaCoffee/.plans/SC-66-execution-plan.md"

slices:
  - id: S1
    title: "Trace and fix candidate selection pipeline"
    status: pending
    depends_on: []
    files_create: []
    files_modify:
      - "backend/src/szimplacoffee/services/recommendations.py"
    files_read_only: []
    checks:
      - "recommendations API returns results"
      - "fix is minimal — no scoring algorithm rearchitecture"
    evidence: []
    notes: "Check: trust_tier filter thresholds, offer_snapshot recency window, VariantDealFact join requirement"
  - id: S2
    title: "Add regression test"
    status: pending
    depends_on: ["S1"]
    files_create: []
    files_modify:
      - "backend/tests/"
    files_read_only: []
    checks:
      - "pytest -k recommendation passes"
    evidence: []
    notes: "Test should seed at least 1 merchant, 1 product, 1 offer_snapshot"

delivery:
  branch: ""
  commits: []
  pr_url: ""
  started_at: ""
  completed_at: ""
  verification:
    required_passed: false
    commands_run: []
    evidence_refs: []

notes:
  - "Do NOT lower quality thresholds below meaningful levels — wait is valid when inventory >= 900g"
